import React, { useEffect, useState } from 'react';
import { api } from '../api';
import ErrorMessage from '../components/ErrorMessage';
export default function AdminPage(){const [doctors,setDoctors]=useState([]);const [form,setForm]=useState({name:'',email:'',password:'Password@123',specialization:''});const [error,setError]=useState('');const [message,setMessage]=useState('');const load=async()=>{try{setDoctors((await api.doctors()).doctors)}catch(e){setError(e.message)}};useEffect(()=>{load()},[]);const submit=async e=>{e.preventDefault();setError('');setMessage('');try{await api.createDoctor(form);setMessage('Doctor created successfully');setForm({name:'',email:'',password:'Password@123',specialization:''});load()}catch(e){setError(e.message)}};return <section><div className="page-title"><div><h1>Administration</h1><p>Manage doctors for your hospital.</p></div><span className="role-pill">Admin</span></div><div className="grid two"><form className="card form-card" onSubmit={submit}><h2>Add doctor</h2><label>Name</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><label>Email</label><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><label>Password</label><input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><label>Specialization</label><input value={form.specialization} onChange={e=>setForm({...form,specialization:e.target.value})}/><ErrorMessage message={error}/>{message&&<div className="alert success">{message}</div>}<button className="primary">Create doctor</button></form><div className="card"><h2>Doctors</h2>{doctors.map(d=><div className="token-row" key={d._id}><strong>{d.doctorName}</strong><span>{d.specialization||'General'}</span><small>{d.status}</small></div>)}</div></div></section>}
